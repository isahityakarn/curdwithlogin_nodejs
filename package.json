{
  "name": "crud-with-login",
  "version": "1.0.0",
  "description": "Node.js CRUD application with user management and migrations",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "dev:watch": "nodemon --watch server.js --watch routes --watch controllers --watch middleware --watch models --watch config server.js",
    "dev:verbose": "nodemon --verbose server.js",
    "migrate": "node migrations/migrate.js",
    "migrate:rollback": "node migrations/rollback.js",
    "migrate:dev": "npm run migrate && npm run dev"
  },
  "keywords": ["nodejs", "crud", "mysql", "migration"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "mysql2": "^3.6.5",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express-validator": "^7.0.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
